spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://mogadb.cidtckobgf3m.ap-northeast-2.rds.amazonaws.com:3306/mogadb
    username: root
    password: cksdl30104
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: ture
logging:
  level:
    org.hibernate.SQL: debug

